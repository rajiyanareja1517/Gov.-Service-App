
import 'dart:ui';

class URLModel{
  String? title;
  String? web_url;
  String? img_url;
  Color? color;

  URLModel({required this.title,required this.web_url,required this.img_url,required this.color});

}